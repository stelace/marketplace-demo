asset:
  checkout_action:
    en: |
      {marketplaceType, select,
        ecommerce {Proceed to payment}
        other {{timeBased, select,
          true {Book}
          false {Buy}
          other {Buy}
          undefined {Contact}
        }}
      }
    fr: |
      {marketplaceType, select,
        ecommerce {Procéder au paiement}
        other {{timeBased, select,
          true {Réserver}
          false {Acheter}
          other {Acheter}
          undefined {Contacter}
        }}
      }
